# onlinePainDecodingGUI
This is a GUI program for online BMI pain detection. Includes neural signal reading module (from Plexon), algorithm module (Poisson linear dynamic system), stimulation output module (to laser generator). The program can not only configure parameters through .ini files, but also change parameters in real time through the UI during the task.
